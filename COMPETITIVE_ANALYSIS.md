# 竞品特性分析与改进建议

## 一、核心竞品对比

| 特性维度 | 本记事本 | Obsidian | Notion | Logseq | 思源笔记 |
|---------|---------|----------|--------|--------|---------|
| **数据存储** | SQLite 本地 | Markdown 本地 | 云端 | Markdown 本地 | 本地数据库 |
| **编辑器** | Lexical (富文本) | Markdown | Block 编辑器 | 大纲式 | 块级实时渲染 |
| **双向链接** | ✅ WikiLink | ✅ | ✅ | ✅ | ✅ |
| **知识图谱** | ❌ | ✅ | ❌ | ✅ | ✅ |
| **反向链接** | ✅ | ✅ | ✅ | ✅ | ✅ |
| **标签系统** | ✅ | 插件 | ✅ | 插件 | ✅ |
| **版本历史** | ✅ | 插件 | ✅ | ❌ | ✅ |
| **全文搜索** | ✅ FTS5 | ✅ | ✅ | ✅ | ✅ |
| **文件夹树** | ✅ 拖拽排序 | 文件树 | 侧边栏 | 日志为主 | 文档树 |
| **导出格式** | PDF/Markdown/DOCX | PDF/Markdown | 多格式 | Markdown | Markdown/PDF |
| **插件生态** | ❌ | 2000+ 插件 | 丰富 API | 150+ 插件 | 插件市场 |
| **白板/画布** | ❌ | Canvas | ❌ | Whiteboard | 画板 |
| **数据库/表格** | ❌ | 插件 | ✅ 核心功能 | 插件 | 属性视图 |
| **专注模式** | ✅ | 插件 | ❌ | ❌ | ✅ |
| **离线可用** | ✅ | ✅ | 受限 | ✅ | ✅ |
| **多端同步** | ❌ | 付费同步 | ✅ | 付费同步 | 付费同步 |
| **开源** | ✅ | 闭源 | 闭源 | ✅ | 开源 |

## 二、已具备的优势

1. **本地优先 + 开源**：数据完全本地存储，隐私安全，无厂商锁定
2. **Lexical 富文本编辑器**：支持表格、代码块、待办、嵌入等丰富节点
3. **FTS5 全文搜索**：高性能本地搜索，支持内容检索
4. **版本历史**：内置版本管理，无需插件
5. **反向链接**：支持 WikiLink 双向链接和反向链接面板
6. **专注模式**：沉浸式写作体验
7. **拖拽排序**：文件树支持拖拽移动和排序
8. **Electron 桌面端**：跨平台桌面应用，支持 PDF 导出

## 三、关键缺陷与改进建议

### P0 - 高优先级（核心体验缺失）

#### 1. 知识图谱可视化
- **现状**：仅有反向链接面板，无全局知识图谱
- **竞品**：Obsidian/Logseq/思源均提供图形化知识关系网
- **建议**：集成 D3.js 或 React Flow，基于 WikiLink 关系生成力导向图
- **实现路径**：
  1. 后端新增 `/api/graph` 接口，返回节点和边数据
  2. 前端新增 `GraphPanel` 组件，使用 D3 force simulation
  3. 支持点击节点跳转、缩放、拖拽

#### 2. 每日笔记（Daily Notes）
- **现状**：无自动创建每日笔记功能
- **竞品**：Obsidian/Logseq 核心功能，思源支持
- **建议**：添加每日笔记快捷入口，自动按日期创建文件
- **实现路径**：
  1. 新增 `DailyNotesPanel` 组件
  2. 支持 `YYYY-MM-DD` 格式自动创建
  3. 日历视图快速导航

#### 3. 块级引用与嵌入
- **现状**：仅支持文件级 WikiLink
- **竞品**：思源/Notion 支持块级引用和嵌入
- **建议**：扩展 WikiLink 语法支持 `[[file#block-id]]`
- **实现路径**：
  1. 后端解析块级引用
  2. 前端渲染嵌入块内容

### P1 - 中优先级（体验增强）

#### 4. 模板系统
- **现状**：无模板功能
- **竞品**：Obsidian/Notion/思源均支持
- **建议**：支持创建和使用笔记模板
- **实现路径**：
  1. 新增 `templates/` 目录
  2. 新建文件时可选择模板
  3. 支持变量替换（如日期、标题）

#### 5. 标签云/标签管理面板
- **现状**：仅有文件级标签选择器
- **竞品**：Obsidian/思源提供标签云和标签管理
- **建议**：新增全局标签管理面板
- **实现路径**：
  1. 新增 `TagCloudPanel` 组件
  2. 显示标签使用频率和关联文件
  3. 支持标签重命名和合并

#### 6. 导入增强
- **现状**：仅支持基础文件导入
- **竞品**：Obsidian 支持导入 Evernote/Notion/Markdown
- **建议**：支持更多格式导入
- **实现路径**：
  1. 支持 `.html` 导入（使用 turndown 转换）
  2. 支持 `.txt` 批量导入
  3. 支持从剪贴板粘贴 Markdown

#### 7. 阅读模式/预览模式
- **现状**：仅编辑模式
- **竞品**：Obsidian 支持编辑/阅读双模式
- **建议**：添加只读预览模式
- **实现路径**：
  1. 新增阅读模式切换按钮
  2. 使用 Lexical 的 `readOnly` 模式
  3. 优化阅读排版（字体、行距、边距）

### P2 - 低优先级（锦上添花）

#### 8. 自定义主题/CSS 片段
- **现状**：仅支持亮/暗主题切换
- **竞品**：Obsidian 支持社区主题和 CSS 片段
- **建议**：支持自定义 CSS 片段
- **实现路径**：
  1. 新增 `custom.css` 文件加载
  2. 提供主题变量自定义

#### 9. 快捷键自定义
- **现状**：固定快捷键
- **竞品**：Obsidian/Notion 支持自定义快捷键
- **建议**：快捷键设置面板
- **实现路径**：
  1. 新增 `ShortcutsSettings` 组件
  2. 存储到 localStorage

#### 10. 统计面板
- **现状**：仅有字数统计
- **竞品**：Obsidian 支持统计面板
- **建议**：显示文件数、总字数、标签数等统计
- **实现路径**：
  1. 新增 `StatsPanel` 组件
  2. 后端提供统计接口

## 四、技术债务与架构改进

### 1. 前端状态管理
- **现状**：纯 React useState/useContext
- **建议**：引入 Zustand 或 Jotai 管理全局状态
- **收益**：减少 prop drilling，提升可维护性

### 2. 后端 API 版本化
- **现状**：无 API 版本控制
- **建议**：添加 `/api/v1/` 前缀
- **收益**：便于未来 API 演进

### 3. 数据库迁移机制
- **现状**：无迁移脚本
- **建议**：引入 goose 或 golang-migrate
- **收益**：支持数据库 schema 版本管理

### 4. 单元测试覆盖
- **现状**：部分组件有测试
- **建议**：补充 logic 层和 DAO 层测试
- **收益**：提升代码质量和重构信心

## 五、总结

本记事本已具备笔记软件的核心功能（编辑、搜索、标签、版本历史、双向链接），在**本地优先、开源、隐私安全**方面具有明显优势。

**最优先需要补充的三大特性**：
1. 知识图谱可视化（提升知识发现能力）
2. 每日笔记（提升日常记录效率）
3. 模板系统（提升内容创建效率）

这些特性将显著提升产品竞争力，使其从"基础记事本"升级为"个人知识管理工具"。

name: Phase 453 source snapshot
on:
  push:
    branches: [feature/ui-redesign-v1]
    paths: [.github/workflows/phase453-source.yml]
permissions:
  contents: read
jobs:
  source:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Export tracked source and verification manifest
        run: |
          git archive --format=tar.gz HEAD > /tmp/phase453-source.tar.gz
          git ls-tree -r HEAD > /tmp/phase453-tree.txt
          git rev-parse HEAD HEAD^{tree} > /tmp/phase453-sha.txt
      - uses: actions/upload-artifact@v4
        with:
          name: phase453-source-${{ github.sha }}
          path: |
            /tmp/phase453-source.tar.gz
            /tmp/phase453-tree.txt
            /tmp/phase453-sha.txt
          retention-days: 1
          if-no-files-found: error

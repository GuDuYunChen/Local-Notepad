name: Collection source snapshot
on:
  push:
    branches: [feature/ui-redesign-v1]
    paths: [.github/workflows/collection-source.yml]
permissions:
  contents: read
jobs:
  source:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Archive tracked source only
        run: |
          git archive --format=tar.gz HEAD > "$RUNNER_TEMP/collection-source.tar.gz"
          git rev-parse HEAD > "$RUNNER_TEMP/collection-sha.txt"
          git ls-tree -r HEAD > "$RUNNER_TEMP/collection-tree.txt"
      - uses: actions/upload-artifact@v7
        with:
          name: collection-source-${{ github.sha }}
          path: |
            ${{ runner.temp }}/collection-source.tar.gz
            ${{ runner.temp }}/collection-sha.txt
            ${{ runner.temp }}/collection-tree.txt
          retention-days: 1
